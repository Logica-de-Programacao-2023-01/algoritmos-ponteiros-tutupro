package main

import (
	"fmt"
)

// Escreva uma função em Go que receba um ponteiro para um struct Livro
// com campos título e autor, e altere o
// título do livro para "Desconhecido" se o autor for "Anônimo".

type Livro struct {
	titulo string
	autor  string
}

func num(pon *Livro, n string) string {

	if n == " " {
		fmt.Printf("O Autor do livro %s não foi indentificado por isso ele é anonimo", pon.titulo)
	}

	
	*pon = n

	return n
}
func main() {
	
}
