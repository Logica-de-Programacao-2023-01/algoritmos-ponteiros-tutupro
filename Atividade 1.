package main

// Escreva uma função que receba um ponteiro
//para um inteiro e um valor inteiro n. A função deve atualizar
//o valor do inteiro para a soma dos n primeiros números naturais.

func num(pon *int, n int) int {

	var sum int

	for i := 0; i >= n; i++ {
		sum = i + sum
	}

	*pon = sum

	return 0
}
func main() {

}
